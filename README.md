# Retired branch archive, 15 September 2026

This is an archival Git object, NOT an integration commit on main and NOT a release.
The parent commits retain five retired branch tips and the read-only cleanup audit.
`branches.json` maps original names to exact recoverable commits. An archive tag
will keep them reachable before the branch refs are removed. No runtime or test
from these retired branches should be executed as current project code.

The complete pre-cleanup branch bundle and native snapshot are retained in
GitHub Actions run 34944130239, artifact 10385714277 (90-day retention), and in
the conversation download. ZIP SHA-256:
`dafccc04088dccabba5ae5b635248e4db578ceaa0778342ff2f7cdc786ae322a`.

Current development continues on main and the open PR127 branch. Original issue
criteria, accepted task states, provider secrets and production are unchanged.
